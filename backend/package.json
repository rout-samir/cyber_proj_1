{
  "name": "simplesecscan-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev --assets=./frontend"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
